#include <iostream>

using namespace std;

int main(){
        string cmd;
        while(cin >> cmd){
                if(cmd == "E") break;
                else
                    ;
        }
        return 0;
}
